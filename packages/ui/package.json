{
  "name": "@wusel-capture/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Local UI primitives (Button, CtaButton, Heading, cn) — only the components used by wusel-capture.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "type-check": "oxlint --type-aware --type-check --ignore-pattern='*.test.*' --ignore-pattern='*.spec.*'"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "@wusel-capture/tsconfig": "workspace:*",
    "@types/react": "^19.2.14"
  },
  "peerDependencies": {
    "react": ">=19.0.0"
  }
}
